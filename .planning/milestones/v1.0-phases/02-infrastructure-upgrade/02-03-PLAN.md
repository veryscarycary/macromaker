---
phase: 02-infrastructure-upgrade
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java
  - android/build.gradle
  - android/gradle.properties
autonomous: true
requirements:
  - NATV-03
  - NATV-04
  - NATV-05

must_haves:
  truths:
    - "MainApplication.java SoLoader.init uses OpenSourceMergedSoMapping"
    - "android/build.gradle sets minSdkVersion to 24 and kotlinVersion to 1.9.24"
    - "android/gradle.properties has newArchEnabled=true"
  artifacts:
    - path: "android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java"
      provides: "Android application entry point with RN 0.76 SoLoader init"
      contains: "OpenSourceMergedSoMapping"
    - path: "android/build.gradle"
      provides: "Root Android build config"
      contains: "minSdkVersion = 24"
    - path: "android/gradle.properties"
      provides: "Gradle build flags"
      contains: "newArchEnabled=true"
  key_links:
    - from: "android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java"
      to: "com.facebook.react.soloader.OpenSourceMergedSoMapping"
      via: "SoLoader.init import + call"
      pattern: "OpenSourceMergedSoMapping"
---

<objective>
Update Android native infrastructure for RN 0.76: fix SoLoader init in MainApplication.java, bump minSdk to 24 and kotlinVersion to 1.9.24 in build.gradle, and enable New Architecture in gradle.properties.

Purpose: RN 0.76 requires `SoLoader.init(this, new OpenSourceMergedSoMapping())` to load the merged `libreactnative.so`. minSdk 24 is the RN 0.76 minimum. New Architecture (`newArchEnabled=true`) must be enabled. Kotlin 2.0 is deferred to Phase 3 per research (RN 0.76 template ships with 1.9.24; 2.0 compat is confirmed only for RN 0.84+).
Output: Three edited Android files.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infrastructure-upgrade/02-RESEARCH.md
@android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java
@android/build.gradle
@android/gradle.properties
</context>

<interfaces>
<!-- Current SoLoader line in MainApplication.java (line 59):
SoLoader.init(this, false);

Target (Java syntax — project uses Java not Kotlin):
SoLoader.init(this, new OpenSourceMergedSoMapping());

Import to ADD:
import com.facebook.react.soloader.OpenSourceMergedSoMapping;

Current build.gradle ext block:
  minSdkVersion = 23
  kotlinVersion = "1.8.0"

Target:
  minSdkVersion = 24
  kotlinVersion = "1.9.24"

Current gradle.properties:
  newArchEnabled=false

Target:
  newArchEnabled=true
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update MainApplication.java SoLoader init to OpenSourceMergedSoMapping</name>
  <files>android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java</files>
  <action>
    Make two changes to `android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java`:

    1. Add import after the existing SoLoader import:
       ```java
       import com.facebook.react.soloader.OpenSourceMergedSoMapping;
       ```
       Place it directly after `import com.facebook.soloader.SoLoader;` (currently line 12).

    2. In the `onCreate()` method, change:
       ```java
       SoLoader.init(this, false);
       ```
       to:
       ```java
       SoLoader.init(this, new OpenSourceMergedSoMapping());
       ```

    Do not change anything else in the file. The `DefaultNewArchitectureEntryPoint.load()` block, ReactHost, and all other code stays as-is.

    Important: This is Java (not Kotlin), so use `new OpenSourceMergedSoMapping()` syntax (with `new`).
  </action>
  <verify>
    <automated>grep -q "OpenSourceMergedSoMapping" /Users/sossboss/Development/macromaker/android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java && grep -q "import com.facebook.react.soloader.OpenSourceMergedSoMapping" /Users/sossboss/Development/macromaker/android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java && ! grep -q "SoLoader.init(this, false)" /Users/sossboss/Development/macromaker/android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MainApplication.java imports OpenSourceMergedSoMapping and passes it to SoLoader.init(). The old `SoLoader.init(this, false)` line is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update android/build.gradle and gradle.properties</name>
  <files>android/build.gradle, android/gradle.properties</files>
  <action>
    Step 1 — In `android/build.gradle`, update the `ext` block:
    - Change `minSdkVersion = 23` to `minSdkVersion = 24`
    - Change `kotlinVersion = "1.8.0"` to `kotlinVersion = "1.9.24"`

    Note: User decision mentioned Kotlin 2.0, but RN 0.76 template uses 1.9.24 and Kotlin 2.0 requires careful Gradle plugin compat that is only confirmed for RN 0.84+. Using 1.9.24 per research recommendation — Kotlin 2.0 will be adopted in Phase 3 with RN 0.84.1.

    Do not change buildToolsVersion, compileSdkVersion, targetSdkVersion, ndkVersion, or the Gradle/React plugin classpath entries.

    Step 2 — In `android/gradle.properties`, change:
    ```
    newArchEnabled=false
    ```
    to:
    ```
    newArchEnabled=true
    ```

    Important: Do NOT enable newArchEnabled until NATV-01 through NATV-04 are all complete. This plan runs in Wave 1 alongside the iOS infra plan (02-02). The New Architecture enable here is correct because the SoLoader change in Task 1 of this plan satisfies NATV-03/04, and NATV-01/02 (iOS) are parallel but independent (Android New Architecture does not depend on iOS changes).
  </action>
  <verify>
    <automated>grep -q "minSdkVersion = 24" /Users/sossboss/Development/macromaker/android/build.gradle && grep -q 'kotlinVersion = "1.9.24"' /Users/sossboss/Development/macromaker/android/build.gradle && grep -q "newArchEnabled=true" /Users/sossboss/Development/macromaker/android/gradle.properties && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>android/build.gradle has minSdkVersion=24 and kotlinVersion=1.9.24. android/gradle.properties has newArchEnabled=true.</done>
</task>

</tasks>

<verification>
- `grep "OpenSourceMergedSoMapping" android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java` returns matches (import + call)
- `grep "minSdkVersion" android/build.gradle` returns `minSdkVersion = 24`
- `grep "newArchEnabled" android/gradle.properties` returns `newArchEnabled=true`
</verification>

<success_criteria>
Android files are in RN 0.76 New Architecture state: SoLoader uses OpenSourceMergedSoMapping, minSdk is 24, kotlinVersion is 1.9.24, newArchEnabled is true. No Android build attempted here — build verification is the checkpoint in Plan 02-06.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-upgrade/02-03-SUMMARY.md`
</output>
