---
phase: 03-rn-0-84-1-final-upgrade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - babel.config.js
  - metro.config.js
  - __tests__/utils.test.ts
autonomous: true
requirements:
  - RNUP-01
  - RNUP-02

must_haves:
  truths:
    - "Environment requirements for Phase 3 are confirmed before the upgrade proceeds"
    - "package.json is aligned to React Native 0.84.1 with matching React and Hermes/tooling versions"
    - "Known high-risk dependencies are explicitly reconciled for the 0.84.1 hop"
    - "The existing Jest regression suite still passes after dependency/config alignment"
  artifacts:
    - path: "package.json"
      provides: "React Native 0.84.1 and matching runtime/tooling dependency targets"
      contains: "\"react-native\": \"0.84.1"
    - path: ".planning/phases/03-rn-0-84-1-final-upgrade/03-RESEARCH.md"
      provides: "Upgrade hot spots and validation architecture"
      contains: "## Validation Architecture"
    - path: ".planning/phases/03-rn-0-84-1-final-upgrade/03-VALIDATION.md"
      provides: "Phase validation contract"
      contains: "Phase 3"
  key_links:
    - from: "package.json"
      to: "react-native"
      via: "dependencies map"
      pattern: "\"react-native\":"
    - from: "package.json"
      to: "react"
      via: "dependencies map"
      pattern: "\"react\":"
    - from: "babel.config.js"
      to: "react-native toolchain"
      via: "babel plugin configuration"
      pattern: "plugin"
---

<objective>
Confirm the Phase 3 toolchain and dependency target matrix, then land the JavaScript-side version alignment needed for the React Native 0.84.1 hop.

Purpose: This plan reduces upgrade risk before touching native build files. It should prove the machine meets RNUP-02, pin exact package targets for RN 0.84.1, reconcile the high-risk dependency cluster (`react-native-svg`, navigation runtime packages, reanimated/worklets, CLI/Metro tooling), and keep the repo in a testable state.
Output: Updated package manifests/config with React Native 0.84.1-aligned dependencies plus a verified environment/tooling baseline.
</objective>

<execution_context>
@/Users/sossboss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-CONTEXT.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-RESEARCH.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-VALIDATION.md
@package.json
@babel.config.js
@metro.config.js
@__tests__/utils.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Phase 3 build environment</name>
  <files>package.json</files>
  <action>
    Verify the local build environment before editing dependencies:

    1. Run `node -v` and confirm Node is `22.11+`.
    2. Run `xcodebuild -version` and confirm Xcode is `16.1+`.
    3. Run `pod --version` and capture the CocoaPods version.
    4. Run `./gradlew -v` from `android/` and capture the Gradle version in use.

    If any requirement is missing:
    - Stop and document the gap in the summary rather than proceeding blindly.
    - Do not start dependency edits until the environment mismatch is understood.

    Record the confirmed environment versions in the Phase 3 summary.
  </action>
  <verify>
    <automated>node -v && xcodebuild -version && pod --version</automated>
  </verify>
  <done>Node, Xcode, and CocoaPods requirements for RNUP-02 are either confirmed or an explicit blocker is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Align package versions for RN 0.84.1 and reconcile high-risk dependencies</name>
  <files>package.json, package-lock.json, babel.config.js, metro.config.js</files>
  <action>
    Use the RN 0.84.1 upgrade diff/template as the source of truth for exact runtime/tooling package alignment, then update the local project carefully.

    Steps:
    1. Determine the exact companion versions required with `react-native@0.84.1` for:
       - `react`
       - Hermes/runtime alignment required by the RN 0.84.1 template and lockfile
       - `@react-native/metro-config`
       - `@react-native-community/cli`
       - `@react-native-community/cli-platform-ios`
       - `@react-native-community/cli-platform-android`
    2. Check the compatible versions needed for the current runtime dependency cluster:
       - `react-native-screens`
       - `react-native-safe-area-context`
       - `react-native-gesture-handler`
       - `react-native-reanimated`
       - `react-native-worklets` if required
       - `react-native-svg`
    3. Update `package.json` and regenerate `package-lock.json`.
    4. If Metro/Babel config must change for the new toolchain, make only the minimal required edits.
    5. If `react-native-svg` requires a temporary patch to diagnose build issues, document that fact but do not treat it as an acceptable end state for the phase.

    Guardrails:
    - Keep the target exact: `react-native@0.84.1`.
    - Do not broaden the target to another `0.84.x`.
    - Do not apply native project template rewrites in this plan; those belong in Plan 03-02.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; console.log('rn', deps['react-native']); console.log('react', deps['react']); console.log('svg', deps['react-native-svg']); console.log('reanimated', deps['react-native-reanimated']);" && (test -f ios/Podfile.lock && grep -i hermes ios/Podfile.lock | head -5 || true)</automated>
  </verify>
  <done>package.json is aligned to React Native 0.84.1 with matching React/Hermes/tooling versions and the high-risk package cluster is explicitly reconciled.</done>
</task>

<task type="auto">
  <name>Task 3: Run automated regression checks after version alignment</name>
  <files>__tests__/utils.test.ts</files>
  <action>
    Run the existing automated checks after the dependency/config edits:

    1. Run `npx jest __tests__/utils.test.ts --watchAll=false`.
    2. If that passes, run `npm run test -- --watchAll=false`.
    3. If tests fail due to legitimate upgrade fallout, fix only issues required to keep the existing suite green.

    Capture any remaining manual-only risks for Plan 03-02 or Plan 03-03 rather than over-expanding this plan.
  </action>
  <verify>
    <automated>npm run test -- --watchAll=false</automated>
  </verify>
  <done>The existing Jest suite passes after the Phase 3 dependency/config alignment work.</done>
</task>

</tasks>

<verification>
- `node -v` confirms Node `22.11+` or a blocker is explicitly documented
- `xcodebuild -version` confirms Xcode `16.1+` or a blocker is explicitly documented
- `node -e "console.log(require('./package.json').dependencies['react-native'])"` returns `0.84.1`
- Hermes alignment is evidenced from the resolved iOS/Android dependency state after install
- `npm run test -- --watchAll=false` exits successfully
</verification>

<success_criteria>
RNUP-02 environment requirements are explicitly confirmed. package.json/package-lock reflect an exact React Native 0.84.1 dependency alignment with matching React/Hermes expectations. The high-risk package cluster is reconciled early, and the existing automated suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rn-0-84-1-final-upgrade/03-01-SUMMARY.md`
</output>
