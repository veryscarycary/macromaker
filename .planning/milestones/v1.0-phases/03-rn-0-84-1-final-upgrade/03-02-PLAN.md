---
phase: 03-rn-0-84-1-final-upgrade
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - ios/Podfile
  - ios/Podfile.lock
  - ios/macromaker/AppDelegate.mm
  - ios/macromaker/AppDelegate.h
  - ios/macromaker.xcodeproj/project.pbxproj
  - android/build.gradle
  - android/app/build.gradle
  - android/gradle.properties
  - android/gradle/wrapper/gradle-wrapper.properties
  - android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java
autonomous: true
requirements:
  - RNUP-01
  - RNUP-03
  - RNUP-04

must_haves:
  truths:
    - "The iOS native project is reconciled with the RN 0.84.1 template without losing required local customizations"
    - "The Android native project is reconciled with the RN 0.84.1 template without regressing New Architecture support"
    - "The app launches on iOS simulator and Android emulator at React Native 0.84.1"
    - "Any temporary compatibility patch used during diagnosis is either removed or explicitly treated as a blocker for final completion"
  artifacts:
    - path: "ios/Podfile.lock"
      provides: "Resolved iOS dependencies for the final RN 0.84.1 hop"
      contains: "React"
    - path: "android/gradle.properties"
      provides: "Android New Architecture and Hermes settings after final-hop reconciliation"
      contains: "newArchEnabled=true"
    - path: "package.json"
      provides: "Exact RN 0.84.1 runtime version"
      contains: "\"react-native\": \"0.84.1"
  key_links:
    - from: "ios/Podfile"
      to: "React Native iOS template"
      via: "use_react_native! and post_install configuration"
      pattern: "react_native_post_install"
    - from: "android/app/build.gradle"
      to: "React Native Android template"
      via: "react block and dependencies"
      pattern: "com.facebook.react"
---

<objective>
Apply the iOS and Android native/config reconciliation required for the React Native 0.84.1 hop, then stabilize simulator/emulator builds.

Purpose: This plan turns the version-aligned dependency set from Plan 03-01 into working native projects. It must reconcile official RN 0.84.1 template diffs with the repo’s existing Phase 2 customizations, preserve BootSplash/New Architecture behavior, and achieve repeatable iOS simulator and Android emulator launch success.
Output: Updated native/config files and successful simulator/emulator launches on React Native 0.84.1.
</objective>

<execution_context>
@/Users/sossboss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-CONTEXT.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-RESEARCH.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-VALIDATION.md
@.planning/phases/02-infrastructure-upgrade/02-06-SUMMARY.md
@ios/Podfile
@android/build.gradle
@android/app/build.gradle
@android/gradle.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile iOS native files with the RN 0.84.1 template</name>
  <files>ios/Podfile, ios/macromaker/AppDelegate.mm, ios/macromaker/AppDelegate.h, ios/macromaker.xcodeproj/project.pbxproj</files>
  <action>
    Compare the current iOS project files against the official RN 0.84.1 template diff and apply only the required final-hop changes.

    Required checks:
    1. Confirm `AppDelegate.mm` / `AppDelegate.h` still match the current RN template expectations for `RCTAppDelegate`.
    2. Confirm BootSplash integration remains valid after the final-hop template diff.
    3. Reconcile `ios/Podfile` with the RN 0.84.1 template while preserving any still-needed custom post_install logic from Phase 2.
    4. Update `project.pbxproj` only if the template or build system requires it.

    Guardrails:
    - Do not delete the custom CocoaPods workaround unless the upgraded pod graph proves it is obsolete.
    - If the workaround is still needed, adapt it instead of starting over from a clean template.
  </action>
  <verify>
    <automated>cd ios && pod install</automated>
  </verify>
  <done>The iOS native project is aligned with the RN 0.84.1 template and `pod install` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Android native/config files with the RN 0.84.1 template</name>
  <files>android/build.gradle, android/app/build.gradle, android/gradle.properties, android/gradle/wrapper/gradle-wrapper.properties, android/app/src/main/java/com/carymeskell/macrotracker/MainApplication.java</files>
  <action>
    Compare the current Android project against the RN 0.84.1 template and update only the lines required for the final hop.

    Required checks:
    1. Confirm Gradle/AGP/Kotlin versions match RN 0.84.1 expectations.
    2. Confirm `newArchEnabled=true` and Hermes settings remain valid.
    3. Reconcile `MainApplication` / package initialization with the current template.
    4. Update the Gradle wrapper if the official template requires it.

    If the app is still using `MainApplication.java` rather than `.kt`, adapt the task to the actual file in the repo and keep the file extension consistent with the existing project.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug</automated>
  </verify>
  <done>The Android native project is aligned with the RN 0.84.1 template and `assembleDebug` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Prove simulator and emulator launch on the final-hop native state</name>
  <files>ios/Podfile.lock</files>
  <action>
    After native reconciliation:

    1. Run `npm run ios` and confirm the app launches on an iOS simulator without crash.
    2. Run `npm run android` and confirm the app launches on an Android emulator without crash.
    3. If launch failures are caused by dependency compatibility fallout (especially `react-native-svg`), diagnose and fix them here only if the result does not leave a permanent temporary patch behind.

    Capture any remaining flow-level verification work for Plan 03-03 rather than expanding this plan into the full manual checklist.
  </action>
  <verify>
    <automated>npm run test -- --watchAll=false</automated>
  </verify>
  <done>Both iOS simulator and Android emulator can launch the app at React Native 0.84.1, and the repo remains in a green automated-test state.</done>
</task>

</tasks>

<verification>
- `cd ios && pod install` succeeds
- `cd android && ./gradlew assembleDebug` succeeds
- iOS simulator launch succeeds with `npm run ios`
- Android emulator launch succeeds with `npm run android`
</verification>

<success_criteria>
The native iOS and Android projects are reconciled with the RN 0.84.1 template while preserving required local customizations. Simulator and emulator launches both succeed on the final-hop dependency set.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rn-0-84-1-final-upgrade/03-02-SUMMARY.md`
</output>
