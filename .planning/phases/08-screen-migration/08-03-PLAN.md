---
phase: 08-screen-migration
plan: 03
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - screens/AddFood/AddFoodScreen.tsx
  - screens/AddFood/components/MacroInput.tsx
autonomous: true
requirements:
  - MIGR-02

must_haves:
  truths:
    - "AddFoodScreen and MacroInput no longer import from components/Themed"
    - "AddFoodScreen smoke render test passes after migration"
    - "The inline macro proportion bar in AddFoodScreen is left unchanged (not replaced with MacroProgressBar)"
  artifacts:
    - path: "screens/AddFood/AddFoodScreen.tsx"
      provides: "Add/Edit food screen with DS Text"
      contains: "import { Text } from '../../design/components'"
    - path: "screens/AddFood/components/MacroInput.tsx"
      provides: "Macro input subcomponent with DS Text"
      contains: "import { Text } from '../../../design/components'"
  key_links:
    - from: "screens/AddFood/AddFoodScreen.tsx"
      to: "design/components"
      via: "import { Text }"
      pattern: "from.*design/components"
---

<objective>
Migrate AddFoodScreen and its MacroInput subcomponent from `components/Themed` to the design system. Both screens already use token imports extensively — the remaining work is replacing `Themed.Text`/`Themed.View` with DS Text and plain RN View.

Purpose: MIGR-02 — the Add Food flow is the highest-frequency user action; DS typography must be consistent here.
Output: Two migrated files. Inline macro proportion bar stays unchanged (it uses colors.macro.* already and has different semantics than MacroProgressBar).
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-screen-migration/08-RESEARCH.md
@.planning/phases/08-screen-migration/08-01-SUMMARY.md

<interfaces>
<!-- DS Text import — always from barrel: -->
```typescript
import { Text } from '../../design/components';       // from AddFoodScreen.tsx depth
import { Text } from '../../../design/components';    // from MacroInput.tsx depth
```

<!-- CRITICAL NOTE from research — DO NOT replace the inline macro bar: -->
<!-- AddFoodScreen has an inline macro bar (3 View segments with flex: carbsCalories) -->
<!-- This shows meal PROPORTIONS only, no target. MacroProgressBar requires target/logged values. -->
<!-- Leave the inline bar as-is — it already uses colors.macro.* tokens correctly. -->

<!-- Current state per research: -->
<!-- AddFoodScreen.tsx: uses Themed.Text + Themed.View; good token usage elsewhere; no hex colors -->
<!-- MacroInput.tsx: uses Themed.Text + Themed.View; token usage otherwise complete -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AddFoodScreen.tsx</name>
  <files>screens/AddFood/AddFoodScreen.tsx</files>
  <action>
Read AddFoodScreen.tsx fully before editing.

Apply the standard Themed migration pattern:

1. **Fix the import line** that imports from `components/Themed`:
   - Remove `Text` and `View` from the Themed import
   - Add `Text` from `'../../design/components'` (confirm depth — AddFoodScreen is at screens/AddFood/)
   - Replace `View` with plain RN `View` — merge into existing `react-native` import

2. **DS Text variants** — inspect each `<Text>` element's stylesheet entry:
   - Screen title or section headers → `variant="subheading"` or `variant="heading"`
   - Form labels and body content → `variant="body"` or `variant="label"`
   - Small helper text → `variant="caption"`
   - Keep existing `style` props — DS Text merges them with variant defaults

3. **Themed.View replacement:**
   - Replace `<View>` (imported from Themed) with `<View>` from react-native
   - No backgroundColor injection needed — all container Views that need white background already have it explicit in styles, or transparency is acceptable

4. **Leave the inline macro bar completely untouched.** The three-segment proportion bar using `flex: carbsCalories` etc. is intentionally NOT replaced with `MacroProgressBar`. Comment if needed: `// Proportion-only bar — not replaced with MacroProgressBar (requires target/logged semantics)`

5. Verify the file compiles by confirming no remaining Themed reference.
  </action>
  <verify>
    <automated>npx jest __tests__/screens/AddFoodScreen.test.tsx --watchAll=false 2>&1 | tail -8</automated>
  </verify>
  <done>AddFoodScreen.test.tsx passes. No Themed import in AddFoodScreen.tsx. Inline macro proportion bar unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MacroInput.tsx</name>
  <files>screens/AddFood/components/MacroInput.tsx</files>
  <action>
Read MacroInput.tsx fully before editing.

MacroInput is a subcomponent at `screens/AddFood/components/MacroInput.tsx`. Depth from root: 3 levels deep.

Apply the same Themed migration pattern:
1. Remove Themed import: `import { Text, View } from '../../../components/Themed'`
2. Add: `import { Text } from '../../../design/components'`
3. Merge View into existing `import { View, ... } from 'react-native'` (or add if missing)
4. Apply appropriate `variant` to each `<Text>` element based on its role:
   - Macro label text (e.g., "Carbs", "Protein") → `variant="label"`
   - Numeric value display → these may use raw RN Text if they need tabular-nums; check if `fontVariant` is set. If not, use `variant="body"`
   - Unit labels ("g", "oz") → `variant="caption"` or `variant="label"`
5. Remove any standalone `fontWeight: 'bold'` where fontFamilies.* is also set in the same style

Verify no Themed reference remains.
  </action>
  <verify>
    <automated>npx jest __tests__/screens/AddFoodScreen.test.tsx --watchAll=false 2>&1 | tail -8 && grep -n "Themed" /Users/sossboss/Development/macromaker/screens/AddFood/components/MacroInput.tsx || echo "No Themed — PASS"</automated>
  </verify>
  <done>MacroInput.tsx has no Themed import. AddFoodScreen test still passes (MacroInput is rendered inside AddFoodScreen).</done>
</task>

</tasks>

<verification>
```bash
# No Themed imports in AddFood screens
grep -rn "Themed" /Users/sossboss/Development/macromaker/screens/AddFood/ || echo "CLEAN"

# AddFoodScreen test passes
npx jest __tests__/screens/AddFoodScreen.test.tsx --watchAll=false

# Full suite
npx jest --watchAll=false
```
</verification>

<success_criteria>
- Zero Themed imports in screens/AddFood/**
- AddFoodScreen.test.tsx passes
- Inline macro proportion bar is unchanged
- Full jest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-screen-migration/08-03-SUMMARY.md`
</output>
