---
phase: 05-token-foundation-font-integration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - __tests__/tokens/colors.test.ts
  - __tests__/tokens/typography.test.ts
  - __tests__/tokens/spacing.test.ts
  - __tests__/tokens/radius.test.ts
  - design/tokens/colors.ts
  - design/tokens/typography.ts
  - design/tokens/spacing.ts
  - design/tokens/radius.ts
  - design/tokens/shadows.ts
  - design/tokens/index.ts
autonomous: true
requirements:
  - TOKS-01
  - TOKS-02
  - TOKS-03
  - TOKS-04

must_haves:
  truths:
    - "Five token files exist in design/tokens/ (colors, typography, spacing, radius, shadows)"
    - "A barrel index at design/tokens/index.ts re-exports every token and its TypeScript type"
    - "All token files pass their unit tests with zero failures"
    - "Color tokens have a primitive palette layer and a semantic layer (brand, surface, text, macro)"
    - "Typography scale has 9 named variants each referencing a distinct Inter fontFamily string"
    - "Spacing tokens follow an 8pt grid with 8 named steps"
    - "Radius tokens have exactly 4 named levels (xs, sm, md, lg)"
  artifacts:
    - path: "design/tokens/colors.ts"
      provides: "Primitive palette + semantic color tokens"
      exports: ["colors", "Colors"]
    - path: "design/tokens/typography.ts"
      provides: "Inter font family names + 9-level type scale"
      exports: ["fontFamilies", "typeScale", "TypeScaleKey", "TypeScale"]
    - path: "design/tokens/spacing.ts"
      provides: "8pt grid spacing constants"
      exports: ["spacing", "Spacing"]
    - path: "design/tokens/radius.ts"
      provides: "4-level border radius constants"
      exports: ["radius", "Radius"]
    - path: "design/tokens/shadows.ts"
      provides: "Shadows stub (deferred to v2)"
      exports: ["shadows"]
    - path: "design/tokens/index.ts"
      provides: "Barrel re-export of all tokens"
    - path: "__tests__/tokens/colors.test.ts"
      provides: "Unit tests for TOKS-01"
    - path: "__tests__/tokens/typography.test.ts"
      provides: "Unit tests for TOKS-02"
    - path: "__tests__/tokens/spacing.test.ts"
      provides: "Unit tests for TOKS-03"
    - path: "__tests__/tokens/radius.test.ts"
      provides: "Unit tests for TOKS-04"
  key_links:
    - from: "design/tokens/index.ts"
      to: "design/tokens/colors.ts"
      via: "named re-export"
      pattern: "export.*from.*./colors"
    - from: "design/tokens/typography.ts"
      to: "fontFamilies"
      via: "typeScale object references fontFamilies values"
      pattern: "fontFamilies\\.(regular|medium|semiBold|bold)"
---

<objective>
Create the five design token files and their unit tests using TDD. Tests are written first (failing), then implementations make them pass.

Purpose: Establish the typed, static token foundation that all downstream phases (Paper theme, component library, screen migration) will import. No runtime library — plain TypeScript `as const` objects with full type exports.
Output: `design/tokens/` directory with 5 token files + barrel index, plus `__tests__/tokens/` with 4 test files — all tests green.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-token-foundation-font-integration/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for all token files (Wave 0)</name>
  <files>__tests__/tokens/colors.test.ts, __tests__/tokens/typography.test.ts, __tests__/tokens/spacing.test.ts, __tests__/tokens/radius.test.ts</files>
  <behavior>
    colors.test.ts:
    - Test: `colors` is exported from `design/tokens/colors`
    - Test: `colors.brand.primary` is a string matching hex pattern `/#[0-9a-fA-F]{6}/`
    - Test: `colors.brand.primaryDark` and `colors.brand.primaryLight` are hex strings
    - Test: `colors.surface` has keys: default, subtle, muted, border — all hex strings
    - Test: `colors.text` has keys: primary, secondary, tertiary, inverse — all hex strings
    - Test: `colors.macro` has keys: carbs, protein, fat — all hex strings
    - Test: No undefined values anywhere in `colors` (deep check via JSON.stringify)

    typography.test.ts:
    - Test: `fontFamilies` exports keys: regular, medium, semiBold, bold
    - Test: Each fontFamily value is a non-empty string
    - Test: `typeScale` exports exactly 9 keys: display, heading, subheading, body, bodyMedium, bodySmall, caption, label, overline
    - Test: Each typeScale entry has `fontFamily` (string), `fontSize` (positive number), `lineHeight` (positive number)
    - Test: `typeScale.overline` has `textTransform` equal to `'uppercase'`
    - Test: `typeScale.overline` has `letterSpacing` that is a positive number

    spacing.test.ts:
    - Test: `spacing` exports exactly 8 keys: xs, sm, md, lg, xl, 2xl, 3xl, 4xl
    - Test: Values are 4, 8, 12, 16, 24, 32, 48, 64 (in that order)
    - Test: All values are positive numbers

    radius.test.ts:
    - Test: `radius` exports exactly 4 keys: xs, sm, md, lg
    - Test: All values are positive numbers
    - Test: Values follow ascending order (xs < sm < md < lg)
  </behavior>
  <action>
    Create `__tests__/tokens/` directory. Write four test files using Jest. Each test file imports from the source file that does NOT yet exist — tests MUST fail at this point.

    Important details:
    - Import paths use relative paths from `__tests__/tokens/` to `design/tokens/`: `../../design/tokens/colors`
    - Do NOT mock anything — these are pure TypeScript unit tests with no React Native dependencies
    - Use `describe` blocks named after the requirement ID (e.g., `describe('TOKS-01: Color tokens', () => {`)
    - The `textTransform` test on overline: use `expect(typeScale.overline.textTransform).toBe('uppercase')`
    - The `letterSpacing` test on overline: use `expect(typeScale.overline.letterSpacing).toBeGreaterThan(0)`
    - For spacing values test: use `expect(Object.values(spacing)).toEqual([4, 8, 12, 16, 24, 32, 48, 64])`
    - For radius ascending test: destructure `const { xs, sm, md, lg } = radius` and assert xs < sm, sm < md, md < lg

    After writing all four test files, run to confirm RED:
    `npx jest __tests__/tokens/ --no-watchAll 2>&1 | tail -20`

    Commit: `test(05-01): add failing token unit tests (Wave 0)`
  </action>
  <verify>
    <automated>npx jest __tests__/tokens/ --no-watchAll 2>&1 | grep -E "FAIL|Cannot find module" | head -10</automated>
  </verify>
  <done>Four test files exist. Running `npx jest __tests__/tokens/ --no-watchAll` produces FAIL results with "Cannot find module" errors — tests are RED as expected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token files to make tests GREEN</name>
  <files>design/tokens/colors.ts, design/tokens/typography.ts, design/tokens/spacing.ts, design/tokens/radius.ts, design/tokens/shadows.ts, design/tokens/index.ts</files>
  <behavior>
    colors.ts: exports `colors` object and `Colors` type
    typography.ts: exports `fontFamilies`, `typeScale`, `TypeScaleKey`, `TypeScale`
    spacing.ts: exports `spacing`, `Spacing`
    radius.ts: exports `radius`, `Radius`
    shadows.ts: exports `shadows` (empty stub with v2 comment)
    index.ts: barrel re-exports all of the above
  </behavior>
  <action>
    Create `design/tokens/` directory. Implement all five token files exactly as specified in `05-RESEARCH.md` Architecture Patterns section.

    **colors.ts** — two-layer pattern (Pattern 1 in research):
    - Private `palette` const with: slate50 through slate900, orange400/500/600, blue400, violet400, amber400, white, black
    - Exported `colors` const with semantic groups: brand (primary/primaryDark/primaryLight), surface (default/subtle/muted/border), text (primary/secondary/tertiary/inverse), macro (carbs/protein/fat)
    - Export `Colors` type: `export type Colors = typeof colors`
    - Mapping: brand.primary = orange500, surface.default = white, text.primary = slate900, macro.carbs = blue400, macro.protein = violet400, macro.fat = amber400

    **typography.ts** — Pattern 2 in research:
    - Export `fontFamilies` with keys: regular='Inter-Regular', medium='Inter-Medium', semiBold='Inter-SemiBold', bold='Inter-Bold'
    - Export `typeScale` with 9 entries. Use exact values from research:
      - display: { fontFamily: fontFamilies.bold, fontSize: 34, lineHeight: 41 }
      - heading: { fontFamily: fontFamilies.bold, fontSize: 28, lineHeight: 34 }
      - subheading: { fontFamily: fontFamilies.semiBold, fontSize: 22, lineHeight: 28 }
      - body: { fontFamily: fontFamilies.regular, fontSize: 16, lineHeight: 24 }
      - bodyMedium: { fontFamily: fontFamilies.medium, fontSize: 16, lineHeight: 24 }
      - bodySmall: { fontFamily: fontFamilies.regular, fontSize: 14, lineHeight: 20 }
      - caption: { fontFamily: fontFamilies.regular, fontSize: 12, lineHeight: 16 }
      - label: { fontFamily: fontFamilies.medium, fontSize: 12, lineHeight: 16 }
      - overline: { fontFamily: fontFamilies.medium, fontSize: 10, lineHeight: 14, letterSpacing: 1.5, textTransform: 'uppercase' as const }
    - Export `TypeScaleKey` and `TypeScale` types

    **spacing.ts** — Pattern 3 in research:
    - Export `spacing` with keys: xs=4, sm=8, md=12, lg=16, xl=24, '2xl'=32, '3xl'=48, '4xl'=64
    - Export `Spacing` type

    **radius.ts** — Pattern 3 in research:
    - Export `radius` with keys: xs=4, sm=8, md=12, lg=16
    - Export `Radius` type

    **shadows.ts** — stub:
    ```typescript
    // Shadows deferred to v2 (DS-03). Reserved module slot for Phase 6+ use.
    export const shadows = {} as const;
    export type Shadows = typeof shadows;
    ```

    **index.ts** — barrel (Pattern 4 in research):
    - Re-export colors, Colors, fontFamilies, typeScale, TypeScaleKey, TypeScale, spacing, Spacing, radius, Radius, shadows, Shadows

    After writing all files, run tests:
    `npx jest __tests__/tokens/ --no-watchAll`

    All 4 test suites must be GREEN. Fix any type errors before committing.

    Commit: `feat(05-01): implement design token files`

    Then run full suite to confirm no regressions:
    `npm run test -- --watchAll=false`
  </action>
  <verify>
    <automated>npx jest __tests__/tokens/ --no-watchAll 2>&1 | tail -15</automated>
  </verify>
  <done>All four token test suites pass (PASS). `npx jest __tests__/tokens/ --no-watchAll` exits 0 with all tests green. Full test suite (`npm run test -- --watchAll=false`) also passes with no regressions.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `npx jest __tests__/tokens/ --no-watchAll` — all 4 suites green
2. `npm run test -- --watchAll=false` — full suite green, no regressions
3. `ls design/tokens/` — shows: colors.ts, typography.ts, spacing.ts, radius.ts, shadows.ts, index.ts
4. TypeScript check: `npx tsc --noEmit` — no errors from new token files
</verification>

<success_criteria>
- `design/tokens/` contains 6 files (5 token files + index.ts)
- `__tests__/tokens/` contains 4 test files
- All token unit tests pass (TOKS-01, TOKS-02, TOKS-03, TOKS-04 verified)
- No TypeScript errors in token files
- Full test suite green — no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-token-foundation-font-integration/05-01-SUMMARY.md` with:
- Files created
- Token values (key color hex codes, spacing values, type scale sizes)
- Any deviations from the plan
- Test results
</output>
