---
phase: 06-paper-theme-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - App.tsx
autonomous: false
requirements:
  - PAPR-03
must_haves:
  truths:
    - "App.tsx imports paperTheme from design/theme/paperTheme — not MD3LightTheme/MD3DarkTheme"
    - "PaperProvider receives the imported paperTheme object (not a colorScheme-conditional)"
    - "Searchbar active indicator on AddFoodScreen is orange (#f97316), not Paper default purple"
    - "Paper components on AddFoodScreen use Inter (not Roboto) on Android"
    - "The app builds and launches without error on iOS and Android"
  artifacts:
    - path: "App.tsx"
      provides: "Root component wiring paperTheme into PaperProvider"
      contains: "import { paperTheme } from './design/theme/paperTheme'"
  key_links:
    - from: "App.tsx"
      to: "design/theme/paperTheme.ts"
      via: "import { paperTheme } from './design/theme/paperTheme'"
      pattern: "from '\\.\/design\/theme\/paperTheme'"
    - from: "App.tsx PaperProvider"
      to: "paperTheme object"
      via: "<PaperProvider theme={paperTheme}>"
      pattern: "theme=\\{paperTheme\\}"
---

<objective>
Wire `paperTheme` from Plan 01 into `App.tsx`'s `PaperProvider`, replacing the colorScheme-conditional theme. Then visually verify that AddFoodScreen Paper components (Searchbar, TextInput) show brand orange and Inter typography on both platforms.

Purpose: This is the integration step — the theme object built in Plan 01 has no effect on the app until PaperProvider receives it. This plan closes that gap and confirms visual correctness.
Output: Updated `App.tsx`, visual sign-off checkpoint on iOS simulator and Android emulator.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-paper-theme-integration/06-RESEARCH.md
@.planning/phases/06-paper-theme-integration/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Current App.tsx (lines 1-38) — executor must update this exactly -->
```typescript
// Current theme lines in App.tsx (to be replaced):
import { MD3DarkTheme, MD3LightTheme, PaperProvider } from 'react-native-paper';
// ...
const paperTheme = colorScheme === 'dark' ? MD3DarkTheme : MD3LightTheme;
// ...
<PaperProvider theme={paperTheme}>

// After update:
import { paperTheme } from './design/theme/paperTheme';
import { PaperProvider } from 'react-native-paper';
// Remove: MD3DarkTheme, MD3LightTheme from the import (if not used elsewhere)
// Remove: const paperTheme = colorScheme === ... line
// Keep:  <PaperProvider theme={paperTheme}> (same JSX, different source)
```

<!-- colorScheme audit note (from RESEARCH.md Open Questions #2) -->
// colorScheme is also passed to Navigation: <Navigation colorScheme={colorScheme} />
// Keep `useColorScheme` hook and `colorScheme` variable — only stop using it for Paper theme selection.
// The `const paperTheme = colorScheme === 'dark' ? ...` line is the ONLY line to remove/replace.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update App.tsx to use brand paperTheme</name>
  <files>App.tsx</files>
  <action>
    Make the following targeted changes to App.tsx:

    1. REPLACE the react-native-paper import line:
       Before: `import { MD3DarkTheme, MD3LightTheme, PaperProvider } from 'react-native-paper';`
       After:  `import { PaperProvider } from 'react-native-paper';`
       (Remove MD3DarkTheme and MD3LightTheme — they are no longer needed)

    2. ADD a new import after the react-native-paper import line:
       `import { paperTheme } from './design/theme/paperTheme';`

    3. REMOVE the conditional theme line entirely:
       `const paperTheme = colorScheme === 'dark' ? MD3DarkTheme : MD3LightTheme;`
       (The imported `paperTheme` constant from step 2 replaces this local const)

    4. KEEP everything else unchanged:
       - `useColorScheme` hook and `colorScheme` variable stay (used by Navigation)
       - `<PaperProvider theme={paperTheme}>` JSX stays — the identifier `paperTheme` now resolves to the import, not the deleted local const

    After editing, verify TypeScript compiles: `npx tsc --noEmit` — must return no errors.
  </action>
  <verify>
    <automated>cd /Users/sossboss/Development/macromaker && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    App.tsx imports `paperTheme` from `./design/theme/paperTheme`. MD3DarkTheme and MD3LightTheme are removed from imports. The colorScheme conditional line is gone. TypeScript compiles with no errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — brand colors and Inter on both platforms</name>
  <action>
    Run the app on both platforms and navigate to AddFoodScreen (Diet tab → "+" button to add food):

    **iOS Simulator:**
    1. `npm run ios` — app should launch without error
    2. Navigate to Diet tab → tap the add meal button to reach AddFoodScreen
    3. Focus the Searchbar — the active indicator underline should be orange (#f97316), not Paper's default purple/violet
    4. The Searchbar placeholder text should render in Inter (note the single-story 'a' and 'g' letterforms vs system San Francisco)
    5. TextInput fields should have a slate-colored border/outline (#e2e8f0) when unfocused

    **Android Emulator:**
    1. `npm run android` — app should launch without error
    2. Same navigation to AddFoodScreen
    3. Focus the Searchbar — orange active indicator (same as iOS)
    4. CRITICAL: Verify Inter renders on Android (Roboto has a two-story 'a' with a serif-like top; Inter has a single-story 'a'). The Searchbar placeholder text must use Inter letterforms.
    5. TextInput borders should use slate color, not Paper's default purple
  </action>
  <verify>
    <automated>MISSING — visual rendering on device cannot be automated; human inspection required</automated>
  </verify>
  <done>
    App launches on both platforms without crash. Searchbar active indicator is orange, not purple. Text in Paper components looks like Inter (single-story 'a'), not Roboto (two-story 'a'). No visible regressions on other screens.
  </done>
  <what-built>
    Plan 01 built `design/theme/paperTheme.ts` with brand colors (slate+orange) and Inter fonts wired into react-native-paper via configureFonts. Plan 02 Task 1 wired it into App.tsx's PaperProvider. The Paper Searchbar and TextInput in AddFoodScreen should now show orange active states and Inter typography instead of Paper defaults.
  </what-built>
  <resume-signal>Type "approved" if brand colors and Inter are visible on both platforms. Describe any issues (wrong color, wrong font, crash, etc.) if not approved.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (automated):
- `npx tsc --noEmit` — no TypeScript errors
- `grep "MD3DarkTheme\|MD3LightTheme" App.tsx` — no output (removed)
- `grep "paperTheme" App.tsx` — shows import line and PaperProvider usage
- `npx jest __tests__/theme/paperTheme.test.ts -x --no-coverage` — still all green

After Task 2 (visual checkpoint — human required):
- Orange active indicator on Searchbar (iOS + Android)
- Inter typography in Paper components (iOS + Android)
- No regressions on other screens
</verification>

<success_criteria>
- App.tsx uses imported `paperTheme` — no colorScheme-conditional Paper theme selection
- TypeScript compiles clean
- AddFoodScreen Searchbar active indicator is orange (#f97316) on both platforms
- Paper components render Inter typography on Android (visual confirmation, no Roboto fallback)
- Full test suite passes: `npm test`
</success_criteria>

<output>
After completion, create `.planning/phases/06-paper-theme-integration/06-02-SUMMARY.md`
</output>
