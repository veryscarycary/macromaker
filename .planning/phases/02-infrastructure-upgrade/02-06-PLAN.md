---
phase: 02-infrastructure-upgrade
plan: "06"
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - ios/Podfile.lock
autonomous: false
requirements:
  - NATV-06

must_haves:
  truths:
    - "iOS app builds and launches on simulator at RN 0.76.x"
    - "No bridge-mode warnings in Metro output at iOS launch"
    - "Meal entry, diet history, and graphs all function on iOS"
    - "Android app builds and launches on emulator"
  artifacts:
    - path: "ios/Podfile.lock"
      provides: "Resolved iOS pod dependencies after pod install"
      contains: "React-Core"
  key_links:
    - from: "npm run ios"
      to: "iOS simulator"
      via: "xcodebuild via @react-native-community/cli"
      pattern: "pod install"
---

<objective>
Run pod install, build iOS, verify New Architecture is active, exercise animated interactions, then build and launch Android.

Purpose: All code changes from Plans 02-02 through 02-05 land in a single verification gate. Human confirmation required because native builds cannot be automated and visual/interactive verification of Reanimated 4 and navigation gestures requires human eyes.
Output: Phase 2 infrastructure confirmed working on both platforms.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-infrastructure-upgrade/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pod install and full test suite</name>
  <files>ios/Podfile.lock</files>
  <action>
    Run pod install to resolve iOS dependencies at the new RN 0.76.x + iOS 15.1 targets:
    ```bash
    cd /Users/sossboss/Development/macromaker/ios && pod install
    ```

    If pod install fails due to a spec not found or platform version mismatch:
    - Check the error for which pod is failing
    - Common cause: a pod requires iOS 15.1+ but a transitive dependency still declares a lower minimum — check if `pod update --no-repo-update` resolves it
    - If react-native-bootsplash 7.x requires an additional Podfile configuration step, check its README at `node_modules/react-native-bootsplash/README.md`

    After pod install succeeds, run the full test suite:
    ```bash
    cd /Users/sossboss/Development/macromaker && npx jest --watchAll=false
    ```

    If tests fail, diagnose before proceeding to the build step.
  </action>
  <verify>
    <automated>test -f /Users/sossboss/Development/macromaker/ios/Podfile.lock && npx jest --watchAll=false 2>&1 | grep -E "Tests:" | tail -3 && echo "pod install complete, tests passing"</automated>
  </verify>
  <done>Podfile.lock exists (pod install succeeded). Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build and launch iOS simulator</name>
  <files></files>
  <action>
    Build and launch the iOS app on simulator:
    ```bash
    cd /Users/sossboss/Development/macromaker && npm run ios
    ```

    Monitor the Metro output for:
    - No "Running in bridge mode" warning (confirms New Architecture is active)
    - No red screen / crash on launch
    - BootSplash appears and fades correctly

    If the build fails:
    - Check for "AppDelegate.mm" not found in Xcode project — the file was created on disk but may need to be added to the Xcode project file (`.xcodeproj/project.pbxproj`). Use `open ios/macromaker.xcworkspace` and add the file manually, or use the `react-native-xcode` approach.
    - Check for "Cannot find module 'react-native-worklets'" — run `npm install` again to ensure worklets peer dep is installed.
    - Check for any pod linking errors — run `pod install` again.

    If the app launches successfully, verify the BootSplash screen appears and fades. The app should reach the main screen or onboarding flow without crashing.
  </action>
  <verify>
    <automated>MISSING — iOS simulator launch requires human visual verification. See checkpoint below.</automated>
  </verify>
  <done>iOS app builds without error. Simulator launches. App reaches home screen or onboarding flow. No crash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 2 infrastructure upgrade: RN 0.76.x with New Architecture, iOS AppDelegate.mm rewrite, Android SoLoader update, all JS dependencies at target versions, react-native-paper replacing @rneui/themed.
  </what-built>
  <how-to-verify>
    iOS verification (primary):
    1. Confirm app launched without a red screen or crash
    2. Check Metro terminal output — confirm NO line containing "bridge mode" is present
    3. Navigate to the Diet tab — tap "Add Food". Verify the food search bar (Searchbar) renders correctly and accepts input
    4. On the Add Food screen, verify the Meal Name field (Paper TextInput) accepts input
    5. Add a test meal (enter carb/protein/fat values, tap Add Meal) — verify it saves and shows in today's view
    6. Navigate to Diet History (if any entries exist) — verify the list items show day/date with chevron correctly
    7. Navigate to the Fitness tab — verify the macro graphs (BarGraph, MealTimeGraph, TotalCaloriesGraph) render without errors
    8. Swipe to navigate back from a screen — verify gesture-based navigation still works (Reanimated 4 + Gesture Handler 2.30)

    Android verification (secondary):
    9. Run `npm run android` in a separate terminal
    10. Confirm Android emulator launches the app without crash
    11. Verify the same basic flow: navigate tabs, attempt to add a meal

    New Architecture confirmation:
    12. In the Metro terminal, look for startup log output. On RN 0.76 with New Architecture, you should see Fabric renderer messages rather than bridge-mode messages.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues encountered (e.g., "graph crash on iOS", "Android build fails with: [error]")</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- iOS app builds and launches without crash (NATV-06)
- No bridge-mode warnings in Metro (NATV-05 active)
- Animated interactions work: graphs render, navigation gestures work (JSDP-06)
- react-native-paper components render correctly
- Android builds and launches (secondary)
</verification>

<success_criteria>
iOS: App launches at RN 0.76.x with New Architecture active. All existing features work. No bridge-mode warnings.
Android: App builds and launches.
Phase 2 infrastructure upgrade is complete and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-upgrade/02-06-SUMMARY.md`
</output>
