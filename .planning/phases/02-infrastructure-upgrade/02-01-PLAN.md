---
phase: 02-infrastructure-upgrade
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/utils.test.ts
autonomous: true
requirements:
  - JSDP-05

must_haves:
  truths:
    - "removeStoredData test verifies AsyncStorage.removeItem is called with the correct key"
    - "removeStoredData test verifies no error is thrown on successful removal"
  artifacts:
    - path: "__tests__/utils.test.ts"
      provides: "Unit tests for AsyncStorage helper removeStoredData"
      contains: "removeStoredData"
  key_links:
    - from: "__tests__/utils.test.ts"
      to: "utils.ts removeStoredData"
      via: "import from '../utils'"
      pattern: "removeStoredData"
---

<objective>
Add `removeStoredData` unit tests to `__tests__/utils.test.ts` before the AsyncStorage 3.x upgrade.

Purpose: AsyncStorage 3.x drops the callback-based `removeItem` API. Tests must exist and pass against the current callback implementation before the code is converted, then continue passing after the promise conversion in Plan 02-04.
Output: Two new test cases in the existing utils.test.ts `describe('utils')` block.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-infrastructure-upgrade/02-VALIDATION.md
@__tests__/utils.test.ts
@utils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add removeStoredData tests to utils.test.ts</name>
  <files>__tests__/utils.test.ts</files>
  <behavior>
    - Test 1: successful remove — mock AsyncStorage.removeItem resolves; removeStoredData called with key "meals@01/01/2025" completes without throwing
    - Test 2: verify call — mock AsyncStorage.removeItem is called exactly once with the exact key passed to removeStoredData
  </behavior>
  <action>
    Add a new `describe('removeStoredData', ...)` block inside the existing `describe('utils', ...)` wrapper in `__tests__/utils.test.ts`.

    Use the existing `@react-native-async-storage/async-storage/jest/async-storage-mock` mock that Jest auto-mocks (already configured in the project — no setup changes needed).

    Import `removeStoredData` from `'../utils'` at the top of the file alongside the existing imports.

    Import `AsyncStorage` from `'@react-native-async-storage/async-storage'` for spy assertions.

    Test structure:
    ```typescript
    describe('removeStoredData', () => {
      it('should call AsyncStorage.removeItem with the correct key', async () => {
        const key = 'meals@01/01/2025';
        const spy = jest.spyOn(AsyncStorage, 'removeItem');
        await removeStoredData(key);
        expect(spy).toHaveBeenCalledWith(key, expect.any(Function));
        spy.mockRestore();
      });

      it('should complete without throwing on successful remove', async () => {
        await expect(removeStoredData('meals@01/01/2025')).resolves.not.toThrow();
      });
    });
    ```

    Note: The spy assertion uses `expect.any(Function)` because the CURRENT implementation passes a callback. After Plan 02-04 converts to promise style, update the first test to `expect(spy).toHaveBeenCalledWith(key)` (no callback argument). This is documented here so the Plan 02-04 executor knows to update the spy assertion.
  </action>
  <verify>
    <automated>npx jest __tests__/utils.test.ts --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <done>All utils.test.ts tests pass including the two new removeStoredData tests. No watch mode.</done>
</task>

</tasks>

<verification>
`npx jest __tests__/utils.test.ts --watchAll=false` exits 0 with all tests passing.
</verification>

<success_criteria>
Two new tests for `removeStoredData` added to `__tests__/utils.test.ts`. Full utils test suite green. No failing tests introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-upgrade/02-01-SUMMARY.md`
</output>
