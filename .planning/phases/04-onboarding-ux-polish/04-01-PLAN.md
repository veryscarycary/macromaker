---
phase: 04-onboarding-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/InfoContext.test.ts
  - __tests__/components/StepIndicator.test.tsx
  - __tests__/screens/BasicInfoScreen.test.tsx
autonomous: true
requirements:
  - ONBR-01
  - ONBR-03
  - ONBR-04
  - ONBR-05

must_haves:
  truths:
    - "Three test files exist and run to failing RED state (Wave 0 scaffolds)"
    - "InfoContext test asserts defaultValues has age=30, weight=150, heightFeet=5, heightInches=10"
    - "StepIndicator test asserts correct dot count and filled/unfilled split"
    - "BasicInfoScreen test asserts KeyboardAvoidingView and 'lbs' suffix label are present in the render tree"
  artifacts:
    - path: "__tests__/InfoContext.test.ts"
      provides: "Unit tests for InfoContext defaultValues (ONBR-05)"
    - path: "__tests__/components/StepIndicator.test.tsx"
      provides: "Unit tests for StepIndicator dot rendering (ONBR-01)"
    - path: "__tests__/screens/BasicInfoScreen.test.tsx"
      provides: "Unit tests for KAV presence and suffix label presence (ONBR-03, ONBR-04)"
  key_links:
    - from: "__tests__/InfoContext.test.ts"
      to: "context/InfoContext.tsx"
      via: "import defaultValues"
      pattern: "defaultValues"
    - from: "__tests__/components/StepIndicator.test.tsx"
      to: "screens/InfoModal/components/StepIndicator.tsx"
      via: "import StepIndicator"
      pattern: "StepIndicator"
    - from: "__tests__/screens/BasicInfoScreen.test.tsx"
      to: "screens/InfoModal/screens/BasicInfoScreen.tsx"
      via: "import BasicInfoScreen"
      pattern: "BasicInfoScreen"
---

<objective>
Create three test scaffold files that cover ONBR-01, ONBR-03, ONBR-04, and ONBR-05. These tests run RED now (implementation does not yet exist) and turn GREEN after Plans 04-02 and 04-03 complete.

Purpose: Nyquist compliance — every implementation task must have an automated verify command that can pass. Without scaffolds, execute-plan cannot verify completion.
Output: Three test files in `__tests__/`.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-onboarding-ux-polish/04-CONTEXT.md
@.planning/phases/04-onboarding-ux-polish/04-RESEARCH.md

<interfaces>
<!-- Existing test files for reference on jest setup style -->
<!-- From __tests__/utils.test.ts (already exists) — shows import pattern and jest config -->
<!-- jest preset: "react-native" in package.json -->
<!-- AsyncStorage mock at __mocks__/@react-native-async-storage/async-storage.ts (exists) -->

From context/InfoContext.tsx:
```ts
export const defaultValues: Info = {
  name: '',
  age: 0,        // will become 30 after Plan 04-02
  weight: 0,     // will become 150 after Plan 04-02
  heightFeet: 5,
  heightInches: 6,  // will become 10 after Plan 04-02
  gender: 'male',
  activityLevel: 2,
  bmi: 0,
  bmr: 0,
  tdee: 0,
  targetProteinPercentage: 0.3,
  targetCarbsPercentage: 0.5,
  targetFatPercentage: 0.2,
};
```

From types.tsx (Info shape):
```ts
// Info includes: name, age, weight, heightFeet, heightInches, gender, activityLevel, bmi, bmr, tdee, targetProteinPercentage, targetCarbsPercentage, targetFatPercentage
```

StepIndicator props (new component — Plan 04-02 will create it):
```tsx
type Props = {
  totalSteps: number;
  currentStep: number; // 1-indexed; dots 0..(currentStep-1) are filled
};
```

BasicInfoScreen imports (existing):
```tsx
import { KeyboardAvoidingView } from 'react-native'; // Plan 04-03 will add this
// Weight row will gain a View with testID="weight-suffix" containing <Text>lbs</Text>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InfoContext defaultValues test scaffold</name>
  <files>__tests__/InfoContext.test.ts</files>
  <action>
Create `__tests__/InfoContext.test.ts` with a direct import of `defaultValues` from `context/InfoContext`. Write assertions that will FAIL now (current values are age:0, weight:0, heightInches:6) and PASS after Plan 04-02 updates the context.

Test structure:
```ts
import { defaultValues } from '../context/InfoContext';

describe('InfoContext defaultValues', () => {
  it('has age pre-filled to 30', () => {
    expect(defaultValues.age).toBe(30);
  });
  it('has weight pre-filled to 150', () => {
    expect(defaultValues.weight).toBe(150);
  });
  it('has heightFeet set to 5', () => {
    expect(defaultValues.heightFeet).toBe(5);
  });
  it('has heightInches pre-filled to 10', () => {
    expect(defaultValues.heightInches).toBe(10);
  });
  it('leaves name blank (no default)', () => {
    expect(defaultValues.name).toBe('');
  });
});
```

This file has no React imports — pure unit test against an exported constant. No mocks needed (InfoContext.tsx imports storeData from utils.ts which is already mocked via __mocks__).

Run to confirm RED: `npx jest --testPathPattern="InfoContext" --no-coverage`
  </action>
  <verify>
    <automated>npx jest --testPathPattern="InfoContext" --no-coverage 2>&1 | grep -E "FAIL|PASS|Tests:"</automated>
  </verify>
  <done>File exists. Running the test suite shows FAIL (age, weight, heightInches assertions fail against current 0/0/6 values). No syntax errors — the file parses and jest finds the describe block.</done>
</task>

<task type="auto">
  <name>Task 2: StepIndicator component test scaffold</name>
  <files>__tests__/components/StepIndicator.test.tsx</files>
  <action>
Create the `__tests__/components/` directory (if not present) and write `StepIndicator.test.tsx`.

The StepIndicator component does not yet exist — Plan 04-02 will create it at `screens/InfoModal/components/StepIndicator.tsx`. The test import will fail until then, which is correct RED behavior.

Test structure:
```tsx
import React from 'react';
import { render } from '@testing-library/react-native';
import StepIndicator from '../../screens/InfoModal/components/StepIndicator';

describe('StepIndicator', () => {
  it('renders totalSteps number of dots', () => {
    const { getAllByTestId } = render(
      <StepIndicator totalSteps={3} currentStep={1} />
    );
    expect(getAllByTestId('step-dot')).toHaveLength(3);
  });

  it('marks correct number of dots as filled for step 1 of 3', () => {
    const { getAllByTestId } = render(
      <StepIndicator totalSteps={3} currentStep={1} />
    );
    const dots = getAllByTestId('step-dot');
    expect(dots).toHaveLength(3);
    // dot 0 filled (index 0 < currentStep 1), dots 1 and 2 empty
    expect(dots[0].props.testID).toBe('step-dot');
    // Verify via accessibilityLabel set in component
    expect(dots[0].props.accessibilityLabel).toBe('filled');
    expect(dots[1].props.accessibilityLabel).toBe('empty');
    expect(dots[2].props.accessibilityLabel).toBe('empty');
  });

  it('marks all 3 dots filled for step 3 of 3', () => {
    const { getAllByTestId } = render(
      <StepIndicator totalSteps={3} currentStep={3} />
    );
    const dots = getAllByTestId('step-dot');
    dots.forEach(dot => {
      expect(dot.props.accessibilityLabel).toBe('filled');
    });
  });
});
```

**Important:** The StepIndicator component (created in Plan 04-02) must add `testID="step-dot"` and `accessibilityLabel={i < currentStep ? 'filled' : 'empty'}` to each dot View so these tests can query them. Include this requirement in the test comments so Plan 04-02 knows the contract.

Check that `@testing-library/react-native` is in package.json devDependencies before writing. If not present, note it in a comment — it likely is present since the project uses jest with react-native preset.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="StepIndicator" --no-coverage 2>&1 | grep -E "FAIL|PASS|Cannot find module|Tests:"</automated>
  </verify>
  <done>File exists at `__tests__/components/StepIndicator.test.tsx`. Running jest shows FAIL or "Cannot find module" for StepIndicator (both are acceptable RED states — file not yet created by Plan 04-02). No syntax errors in the test file itself.</done>
</task>

<task type="auto">
  <name>Task 3: BasicInfoScreen structural test scaffold</name>
  <files>__tests__/screens/BasicInfoScreen.test.tsx</files>
  <action>
Create the `__tests__/screens/` directory (if not present) and write `BasicInfoScreen.test.tsx`.

This test verifies structural presence of KeyboardAvoidingView (ONBR-03) and the "lbs" suffix label (ONBR-04) in the rendered tree. Use `@testing-library/react-native`.

The test must mock:
- InfoContext (`../context/InfoContext`) — provide a minimal state matching the defaultValues shape
- Navigation prop — pass a mock `navigation` object

Test structure:
```tsx
import React from 'react';
import { render } from '@testing-library/react-native';
import BasicInfoScreen from '../../screens/InfoModal/screens/BasicInfoScreen';

// Mock InfoContext
jest.mock('../../context/InfoContext', () => ({
  Context: React.createContext({
    state: {
      name: '',
      age: 30,
      weight: 150,
      heightFeet: 5,
      heightInches: 10,
      gender: 'male',
      activityLevel: 2,
      bmi: 0,
      bmr: 0,
      tdee: 0,
      targetProteinPercentage: 0.3,
      targetCarbsPercentage: 0.5,
      targetFatPercentage: 0.2,
    },
    setInfoState: jest.fn(),
    setBasicInfoCalculations: jest.fn(),
  }),
}));

const mockNavigation = {
  navigate: jest.fn(),
  goBack: jest.fn(),
  getParent: jest.fn(),
};

describe('BasicInfoScreen', () => {
  it('renders a KeyboardAvoidingView', () => {
    const { UNSAFE_getByType } = render(
      <BasicInfoScreen navigation={mockNavigation as any} />
    );
    const { KeyboardAvoidingView } = require('react-native');
    expect(UNSAFE_getByType(KeyboardAvoidingView)).toBeTruthy();
  });

  it('renders "lbs" suffix label for weight input', () => {
    const { getByText } = render(
      <BasicInfoScreen navigation={mockNavigation as any} />
    );
    expect(getByText('lbs')).toBeTruthy();
  });
});
```

Note: These tests will FAIL until Plan 04-03 adds KeyboardAvoidingView and the "lbs" View to BasicInfoScreen. That is the expected RED state.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="BasicInfoScreen" --no-coverage 2>&1 | grep -E "FAIL|PASS|Cannot find module|Tests:"</automated>
  </verify>
  <done>File exists at `__tests__/screens/BasicInfoScreen.test.tsx`. Jest finds and parses the file. Tests fail (KAV not yet in component, "lbs" not yet in component). No import or syntax errors beyond the expected component failures.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
npx jest --testPathPattern="InfoContext|StepIndicator|BasicInfoScreen" --no-coverage
```

Expected: All three test files are found by jest. Most tests fail (RED is correct at this wave). No syntax errors. The test runner exits without crashing.
</verification>

<success_criteria>
- `__tests__/InfoContext.test.ts` exists, tests run RED (age/weight/heightInches assertions fail against current defaults)
- `__tests__/components/StepIndicator.test.tsx` exists, fails with "Cannot find module" for StepIndicator
- `__tests__/screens/BasicInfoScreen.test.tsx` exists, tests run RED (KAV and lbs not yet in component)
- Full jest suite still passes all previously-passing tests: `npx jest --no-coverage`
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-ux-polish/04-01-SUMMARY.md`
</output>
