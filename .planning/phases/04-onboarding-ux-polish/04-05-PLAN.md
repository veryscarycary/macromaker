---
phase: 04-onboarding-ux-polish
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified: []
autonomous: false
requirements:
  - ONBR-01
  - ONBR-02
  - ONBR-03
  - ONBR-04
  - ONBR-05

must_haves:
  truths:
    - "Progress dots are visible on all 3 onboarding screens at the correct filled/empty states"
    - "Button labels are correct on all 3 screens (WelcomeScreen: 'Get Started', BasicInfoScreen: 'Continue', MoreInfoScreen: 'Get Started')"
    - "All 3 CTA buttons show white text on purple background"
    - "Opening the keyboard on BasicInfoScreen does not obscure any input field or the Continue button"
    - "Weight field shows 'lbs' suffix label permanently visible to the right of the input"
    - "On first launch, weight shows 150, age shows 30, height selector shows 5ft 10in pre-selected"
    - "Tapping Continue with empty name shows red border and 'Name is required' text"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 4 onboarding polish changes across all three onboarding screens on the iOS simulator. Runs the full automated test suite first, then prompts the user to perform interactive checks.

Purpose: Automated tests verify structure; this checkpoint verifies visual appearance, keyboard behavior, and the full user flow — things Jest cannot check.
Output: Phase 4 verification confirmation and evidence capture.
</objective>

<execution_context>
@/Users/sossboss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-onboarding-ux-polish/04-CONTEXT.md
@.planning/phases/04-onboarding-ux-polish/04-03-SUMMARY.md
@.planning/phases/04-onboarding-ux-polish/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite</name>
  <files></files>
  <action>
Run the complete test suite and confirm all tests pass. This includes the Wave 0 scaffolds (InfoContext, StepIndicator, BasicInfoScreen) that should now be GREEN, plus the pre-existing tests (utils.test.ts, MacroGraph.test.tsx).

```bash
npx jest --no-coverage
```

If any tests fail, diagnose and fix before proceeding to the human checkpoint. Do not advance to Task 2 with failing tests.
  </action>
  <verify>
    <automated>npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. Test suite exits with code 0. Output shows no failing test suites.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify full onboarding flow on iOS simulator</name>
  <files></files>
  <action>
Launch the app on the iOS simulator with a clean state (no stored basicInfo) and walk through all three onboarding screens, verifying the checklist items in how-to-verify below.
  </action>
  <verify>
    <automated>MISSING — visual and keyboard behavior requires human verification on simulator</automated>
  </verify>
  <done>All checklist items in how-to-verify are confirmed. User types "approved".</done>
  <what-built>
All Phase 4 onboarding UX polish changes:
- StepIndicator (3 dots) at top of all 3 onboarding screens
- InfoContext defaults: weight=150, age=30, height=5'10"
- BasicInfoScreen: KeyboardAvoidingView, 'lbs' suffix on weight, name validation (red border), button label "Continue"
- WelcomeScreen: SafeAreaView wrapper, button text color white
- MoreInfoScreen: button label "Get Started", button text color white
- All 3 CTA buttons: purple background + white text
  </what-built>
  <how-to-verify>
**Setup:** Reset app storage first to see first-launch onboarding flow.
```bash
# Kill any running Metro instance, then launch fresh:
npm run ios
```
If the app does not show onboarding on launch (stored basicInfo exists), clear AsyncStorage by:
- Going to iOS Simulator menu: Device > Erase All Content and Settings, OR
- Uninstall and reinstall the app on simulator

**Checks to perform:**

1. **WelcomeScreen (Step 1)**
   - [ ] Three dots visible at top: first dot filled, two empty (● ○ ○)
   - [ ] Dots are above the main content area (not inside the scroll)
   - [ ] "Get Started" button: purple background with WHITE text (not black)

2. **BasicInfoScreen (Step 2)**
   - [ ] Three dots visible at top: two filled, one empty (● ● ○)
   - [ ] Weight field: TextInput with "lbs" suffix attached to the right, always visible
   - [ ] Weight pre-filled to 150 (not blank, not 0)
   - [ ] Age pre-filled to 30 (not blank, not 0)
   - [ ] Height selector: 5ft and 10in buttons appear selected (purple highlight)
   - [ ] Tap "Continue" with Name field EMPTY: red border appears on Name input + "Name is required" text below it
   - [ ] Type a name — red border and error text disappear
   - [ ] With name filled: tap "Continue" advances to MoreInfoScreen
   - [ ] **Keyboard test (critical for ONBR-03):** Tap on the Weight or Age input field to open keyboard. Verify that the "Continue" button remains visible and accessible — it is not pushed off screen or hidden behind the keyboard.

3. **MoreInfoScreen (Step 3)**
   - [ ] Three dots visible at top: all three filled (● ● ●)
   - [ ] "Get Started" button: purple background with WHITE text (not black, not "Finish")
   - [ ] Tapping "Get Started" completes onboarding and navigates to the main app

4. **Full flow**
   - [ ] Completing all 3 screens (Welcome → BasicInfo with a name → MoreInfo → Get Started) lands in the main Diet tab without errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
- Full automated test suite exits with code 0
- All human verification checks are approved
- All 5 ONBR requirements confirmed working in the running app
</verification>

<success_criteria>
- `npx jest --no-coverage` exits 0 with all tests green
- Progress indicator shows correct dot states on all 3 screens
- Button labels and text colors correct on all 3 screens
- Keyboard does not obscure BasicInfoScreen content
- Default values appear on first load
- Name validation shows visual feedback
- Full onboarding flow completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-ux-polish/04-05-SUMMARY.md`
</output>
