---
phase: 03-rn-0-84-1-final-upgrade
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - RNUP-03
  - RNUP-04
  - RNUP-05

must_haves:
  truths:
    - "The app launches on iOS simulator and one physical iOS device without crash"
    - "The app launches on Android emulator and one physical Android device without crash"
    - "Core user flows are verified with explicit pass/fail evidence"
    - "Phase 3 does not close with any unresolved core-flow regression or required-device verification gap"
  artifacts:
    - path: ".planning/phases/03-rn-0-84-1-final-upgrade/03-03-SUMMARY.md"
      provides: "Human verification evidence for Phase 3"
      contains: "pass/fail"
  key_links:
    - from: "screens/InfoModal/ModalScreen.tsx"
      to: "onboarding trigger verification"
      via: "modal onboarding stack"
      pattern: "ModalStack.Navigator"
    - from: "utils.ts"
      to: "meal persistence verification"
      via: "AsyncStorage helper layer"
      pattern: "AsyncStorage"
---

<objective>
Run the required final manual verification for React Native 0.84.1, including physical-device proof and a written checklist for all core flows.

Purpose: This is the hard completion gate for Phase 3. It captures the user-required evidence that the app not only builds on RN 0.84.1, but still works on both platforms across the core meal-tracking and onboarding experience.
Output: Human-verified checklist evidence for simulator/emulator plus physical-device launch proof and core-flow behavior.
</objective>

<execution_context>
@/Users/sossboss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sossboss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-CONTEXT.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-RESEARCH.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-VALIDATION.md
@.planning/phases/03-rn-0-84-1-final-upgrade/03-02-SUMMARY.md (if it exists at execution time)
@utils.ts
@screens/InfoModal/ModalScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the final verification checklist and smoke baseline</name>
  <files></files>
  <action>
    Prepare the final manual verification run:

    1. Re-run `npx jest __tests__/utils.test.ts --watchAll=false`.
    2. Re-run `npm run test -- --watchAll=false`.
    3. Write the checklist headings that will be used in the summary:
       - iOS simulator launch
       - iOS physical-device launch
       - Android emulator launch
       - Android physical-device launch
       - onboarding trigger
       - add meal
       - edit meal
       - delete meal
       - today's summary
       - diet history
       - macro graphs
       - tab navigation

    If the automated suite is already red at this point, stop and fix that before starting the human verification checkpoint.
  </action>
  <verify>
    <automated>npm run test -- --watchAll=false</automated>
  </verify>
  <done>The final verification checklist is prepared and the automated baseline is green before human checks begin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full React Native 0.84.1 final-hop upgrade with updated native projects and dependency graph, ready for final user-facing verification.
  </what-built>
  <how-to-verify>
    Required launch proof:
    1. Run `npm run ios` and confirm the app launches on iOS simulator without crash.
    2. Launch the same build on one physical iOS device and confirm startup stability.
    3. Run `npm run android` and confirm the app launches on Android emulator without crash.
    4. Launch the same build on one physical Android device and confirm startup stability.

    Required core-flow checklist:
    5. Onboarding trigger: clear the stored onboarding state if needed, cold launch, confirm the modal flow still appears.
    6. Add meal: create a new meal and verify it appears in today's view.
    7. Edit meal: edit an existing meal and verify the changes persist.
    8. Delete meal: delete a meal and verify it is removed cleanly.
    9. Today's summary: confirm calorie/macro totals update correctly after meal changes.
    10. Diet history: navigate to history and confirm saved entries are still visible and navigable.
    11. Macro graphs: open graph-bearing screens and verify all graphs render correctly without crash or obvious visual corruption.
    12. Tab navigation: move between Diet and Fitness tabs and through modal/stack routes without crashes or broken gestures.

    Evidence rules:
    - Record pass/fail for every line item.
    - Any failed core flow blocks completion.
    - If either physical-device check cannot be performed, the phase remains open.
    - If the app only works with a temporary compatibility patch still in place, the phase remains open.
  </how-to-verify>
  <resume-signal>Type "approved" if every required check passes, or describe the failing items and any missing device proof.</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- iOS simulator and physical-device launch both succeed
- Android emulator and physical-device launch both succeed
- onboarding trigger, meal CRUD, today's summary, history, macro graphs, and tab navigation all pass
- no required-device gap or unresolved core-flow regression remains
</verification>

<success_criteria>
Phase 3 has explicit pass/fail evidence for the required simulator/emulator and physical-device launch checks plus every core flow in scope. Any missing physical-device proof or core-flow regression keeps the phase open.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rn-0-84-1-final-upgrade/03-03-SUMMARY.md`
</output>
